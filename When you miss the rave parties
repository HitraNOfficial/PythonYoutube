import turtle
import time, sys

wn = turtle.Screen()
wn.bgcolor("white")
wn.setup(width=0.99, height=0.99)

for x in range(0, 1000):
    if x % 2 == 0:
        wn.bgcolor("black")
    else:
        wn.bgcolor("white")
    
    time.sleep(0.1)

turtle.done()
